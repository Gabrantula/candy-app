import { AppEditDirective } from './app-edit.directive';

describe('AppEditDirective', () => {
  it('should create an instance', () => {
    const directive = new AppEditDirective();
    expect(directive).toBeTruthy();
  });
});
